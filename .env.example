# Base64-encoded username:bcrypt_hash (generate with: bun server/gen-htpasswd.ts <username>)
SITE_PASSWD=
GUAC_SERVER_PORT=18890
HOST=127.0.0.1

# VNC target (the browser speaks RFB to it through the /vnc/ws proxy)
VNC_HOST=169.254.0.1
VNC_PORT=5901
VNC_PASSWORD=
